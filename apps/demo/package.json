{
  "name": "@commitonce/demo",
  "version": "0.1.0",
  "private": true,
  "description": "CommitOnce A/B demo CLI: the same rebuilt retry, with and without the guard, on devnet",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "demo": "node ./commitonce-demo.ts",
    "dev": "node ./commitonce-demo.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@commitonce/solana": "workspace:*",
    "@solana-program/system": "0.14.1",
    "@solana/kit": "8.3.0"
  }
}
